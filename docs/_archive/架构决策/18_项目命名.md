# 项目命名（提案留档）

> 记录于 2026-07-10。按用户要求将"新名字"先行放入本文档留档。
> ⚠️ 重要：同日用户澄清——项目改名**从未被授权**（此前属误记、误改）。当前决策是**保留旧名** `以太叙事 / AetherNarrator`，新名仅作提案，未落地到代码。

| 项目 | 当前名（已用） | 提案新名（未采用） |
| --- | --- | --- |
| 中文 | 以太叙事 | 界外叙事 |
| 英文 | AetherNarrator | Romancing on the Outer |
| npm 包名 | aethernarrator / AetherNarrator | romancing-on-the-outer |

说明：
- 英文提案为混合大小写，**非全大写**（若采用，副标题由 `AETHERNARRATOR` 改为 `Romancing on the Outer`）。
- git 仓库目录名保持 `AetherNarrator` 不变（用户使用 SourceTree，改目录会乱引用）。
- 本文档仅记录提案；是否采用以用户后续明确指令为准。
