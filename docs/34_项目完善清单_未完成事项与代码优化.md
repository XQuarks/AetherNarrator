# 以太叙事 · 项目完善清单（未完成事项 + 代码优化）

> 创建：2026-07-27
> 性质：**活文档**——汇总 docs/01~33 及未编号方案中「已规划但未落地」的内容，并附代码层可优化点排查结果。
> 维护方式：每完成一项改状态列并补更新日志；新增想法直接追加行。
> 生成依据：逐份文档梳理 + src/ 符号级交叉验证（详见各条目的代码证据列）。

---

## 〇、总体结论

- 文档中规划的方案**绝大多数已落地**：B1-B4（30~33 号）、B6 记忆晋升、知识晋升确认开关、29 号方案 #1~#5、Phase 0-4/7、ui-redesign 批次 1-5、17 号清单 31/35 项、开局剧情主体，均有明确代码证据与测试覆盖。
- 真正的未完成事项集中在四块：
  1. **21 号时间系统进阶待办**（全部 14 项未动工）；
  2. **28 号竞品借鉴总览**的 A3 / C2 / C4 / D1 等尾部项；
  3. ~~**架构评估 Phase 5**（工具调用约束 + L2 分层缓存）~~ ✅ 已落地（2026-07-28，方案 C）；
  4. **20 号对标分析**列出的长期产品级短板（多模型、多插入位 Lorebook、ANN 持久化等）。
- 代码层无 TODO/FIXME 欠账，**循环依赖已全部消除（2026-07-27）**；超大文件拆分已完成第一批（lore-ui 三分 + processTurn 阶段化，2026-07-27），render.js / llm.js / prompt.js 是否继续拆待定。

---

## 一、状态图例

| 状态 | 含义 |
|---|---|
| ⬜ 待办 | 已登记，未开始 |
| 🔵 进行中 | 正在实现 |
| ✅ 已完成 | 代码落地 |
| ⏸ 暂缓 | 主动搁置 |
| ❓ 待确认 | 需人工/产品确认后再定 |

优先级：**P0** 快赢（投入小体感强） · **P1** 重要增强 · **P2** 完整范式 / 探索 · **P3** 远期

---

## 二、文档中未完成的功能事项

### 2.1 时间系统进阶（来源：docs/21，整表待立项）

| ID | 事项 | 说明 | 优先级 | 状态 | 代码现状 |
|---|---|---|---|---|---|
| UI-1 | 自定义历法月历可视化编辑器 | 可视化增删月份/月长/闰月规则，替代手写 JSON | P1 | ✅ 已完成（2026-07-27 用户确认范围 **C 完整版**；编辑器落在 `src/lore-ui.js` `renderTimeConfigSection` 的 custom_calendar 分支，纯函数辅助在 `src/calendar.js`，新增 `test/35-custom-calendar-editor.test.js`（9 项），全绿 387 测试；与引擎彻底解耦，仅读写 `schema.time_config.custom_calendar`） | 引擎已备（`calendar.js` custom_calendar/months 已支持），仅缺编辑器 UI → 现已补 |
| UI-2 | multiverse 多时间线可视化配置器 | 图形化增删时间线、设 mode/起点/active | P1 | ✅ 已完成（2026-07-29 用户确认范围 **C 全功能版**：世界结构切换单/多 + 时间线增删/改名/拖拽排序/活动线 + 每线内嵌自定义历法编辑器（复用 UI-1，按线定位）+ 每线时钟/天气 + 「双界穿梭」模板 + 冲突/校验提示条；代码在 `src/lore-ui.js` `renderMultiverseEditorInner`/`mv*` 动作 + `src/calendar.js` 时间线纯函数，新增 `test/43-multiverse-config.test.js`（14 项），全量 448 测试通过、浏览器烟雾通过） | 引擎已备（`mode:"multiverse"` + `timelines` + `active_timeline`，`normalizeTimeConfig` 已归一化），仅缺 UI → 现已补 |
| UI-3 | 时间流速比 sync_rules | "异界1天=现实1年"类同步规则 | P2 | ✅ 已完成（2026-07-29 用户确认范围 **A 核心三件套**：新增引擎纯函数 `applySyncRules`/`clampSyncRules`/`calendarDayIndex`（`src/calendar.js`），多时间线卡片内可视化增删 `sync_rules`（ref 线 + ratio），游戏主循环推进活动线后按 ratio 同步引用线；代码在 `src/lore-ui.js` `renderMultiverseEditorInner`/`mvAddSync`/`mvDelSync`/`mvSyncRef`/`mvSyncRatio`，新增 `test/44-time-system-advanced.test.js`（14 项），全量 **448** 测试通过） | 引擎层原未实现（calendar.js 无 sync_rules）→ 现已补引擎 + UI |
| UI-4 | 分支/重置策略可视化配置 | 时间穿越默认策略开关 | P2 | ✅ 已完成（2026-07-29 范围 A：世界级 `default_timetravel_strategy`（keep/reset/branch，缺省 keep）+ 每线 `timetravel_strategy` 覆盖；引擎 `resolveTimeTravelStrategy` 三层（指令>线级>世界级>keep 回落），逆跳时套用；编辑器在世界时间体系卡 + 多线卡片；`src/triggers.js` + `src/store.js` + `src/lore-ui.js` `defaultStrategyChanged`/`mvLineStrategy`，全量 448 测试通过） | 原缺世界级默认层（docs/20 §11.3 仅指令+单事件两层）→ 现已补第三层 |
| UI-5 | 事件 retrigger_policy 可视化编辑 | once / repeatable / 次数 / 冷却 | P2 | ✅ 已完成（2026-07-29 范围 A：新增「世界时限/截止事件」编辑器 `deadlineEditorModal`，可视化配 deadlines 的 `retrigger_policy`：一次性(once)/可重复(repeatable+max_repeats+cooldown_steps)；代码在 `src/lore-editors.js` `openDeadlineEditor`/`saveDeadlineReview` + `index.html` 模态 + `src/app.js` 动作，复用 `store.js::normalizeRetriggerPolicy`；全量 448 测试通过） | 引擎已备（`store.js` 已归一化 `retrigger_policy`、`triggers.js::evalPolicy` 已消费），仅缺编辑器 → 现已补 |
| UI-6 | 时间旅行分支回溯/合并面板 | 列出分支、可跳转/合并 | P3 | ⬜ | 无 UI 痕迹 |
| C-1 | 农历闰月/节气显示细化 | lunar 仅月/日，补节气与闰月标注 | P2 | ✅ 已完成（2026-07-29 范围 A 完整：节气 `SOLAR_TERMS` 表 + `formatCalendarDate(showSolarTerms)` 显示层带节气 + AI 注入月份→节气映射；闰月走 custom_calendar「农历(含闰月)」模板（`CUSTOM_CALENDAR_PRESETS.lunarLeap` + 编辑器按钮 + 提示），lunar 模式不引入闰月以保持 12 月进位；新增 `test/45-c1-lunar-solar-terms.test.js`（8 项），全量 **456** 测试通过） | 引擎层 `lunar` 为 decoupled 简化农历，仅做显示标注，不引入真实农历算法（与方案 B 解耦一致） |
| C-2 | 星际历/魔法历纪元换算 UI | cyear 映射"星历第几纪"等展示 | P3 | ⬜ | 无代码痕迹 |
| C-3 | 时区与跨时区时间 | 多世界不同时区统一基准 | P3 | ⬜ | 无代码痕迹 |
| E-1 | 时间线合并（branch merge） | 分支合并回主线 + 触发记录合并规则 | P3 | ⬜ | 无代码痕迹 |
| E-2 | 事件依赖图 | 事件前置/互斥，穿越后依赖重算 | P3 | ⬜ | 无代码痕迹 |
| E-3 | 多世界并行推进性能 | 大量 timeline 时的推进/比较性能 | P3 | ⬜ | 无代码痕迹 |
| T-1 | 进阶 UI 端到端测试脚本 | — | P2 | ⬜ | — |
| T-2 | 时间系统玩法说明书（用户向） | 非技术作者可读，含穿越/多世界写法示例 | P2 | ✅ 2026-07-29 已完成（见 `docs/时间系统玩法说明书.md`） | — |

### 2.2 竞品借鉴尾部项（来源：docs/28）

| ID | 事项 | 说明 | 优先级 | 状态 | 代码现状 |
|---|---|---|---|---|---|
| A3 | 创作完成度清单/进度卡 | 世界详情页加进度卡，不阻塞游玩 | P1 | ✅ 已完成（2026-07-28） | `src/utils.js::computeWorldCompletion` 纯函数从 `world` 现字段派生 7 维度（标题/世界观/开场/≥1角色/游玩系统=rules/重要事件=lore_kb 带 trigger 片段/主角设定=hero），`render.js::showWorldDetail` 概览顶部注入 `.completion-card`（进度条+评级+✓/○ 维度），复用详情页、不新增数据模型、不阻塞游玩；`test/36-world-completion.test.js`（6 项）全绿，总测试 387→393 |
| C2 | 能力目录/升级体系 | 带代价、可选启用 | P2 | ⬜ | 无 ability_catalog 痕迹 |
| C4 | 玩家备注 + 分支预测 | 玩家可写备注影响后续叙事 | P2 | ⬜ | 无 player_note 痕迹 |
| D1 | 文游 vs 小说双模式 | 对话模式/小说模式切换 | P3（远期） | ⬜ | 无痕迹 |
| B5' | 日程系统 + 章节化回溯 | 时间线 Tab 已有，日程/章节回溯未见独立实现 | P2 | ❓ | 时间线 Tab（C5）✅，其余待确认 |（属"时间线/回溯/分支需求族"，见 docs/38 §六；与主追踪 doc 28 B5 同源）
| C1 | 模块化世界开关 | 哪些系统可用由创作者决定 | P2 | ❓ | 关键词过泛未能定向验证 |

### 2.3 IP 一致性方案收尾（来源：docs/29）

| ID | 事项 | 说明 | 优先级 | 状态 |
|---|---|---|---|---|
| #6 | 生成后硬扫描（可选） | 对 LLM 输出做违禁词/设定冲突后置扫描 | P2 | ⬜ |

> #1~#5（一致性包生成 / world.canon 模型 / detectIp / key_divergences / 克苏鲁种子包）均已落地 ✅，证据：`store.js::ensureWorldCanon/resolveCanonContext/applyConsistencyPack`、`llm.js::generateConsistencyPack`、`prompt.js::buildCanonRules`、`utils.js::detectIp`。

### 2.4 架构演进（来源：docs/架构评估与方案脑暴）

| Phase | 事项 | 说明 | 优先级 | 状态 |
|---|---|---|---|---|
| Phase 5 | 工具调用约束 + L2 分层缓存 | LLM 改 function calling 返回结构化状态，降解析失败；角色卡 + 知识库硬约束独立缓存断点降 token | P1 | ✅（2026-07-28，方案 C 已落地，test/37 16 项全绿） |
| Phase 6 | 平台化（后端/账号/云同步） | 与纯前端零后端定位冲突，独立立项 | P3（可选） | ⏸ |

> Phase 0/1/2/3/4/7 均已落地 ✅（ANN 索引、规则 DSL、Critic、KG 图谱、RAG 图遍历、时间系统解耦）。

### 2.5 文档收尾（来源：docs/ui-redesign、docs/17、docs/开局剧情从原文开头）

| ID | 事项 | 说明 | 优先级 | 状态 |
|---|---|---|---|---|
| 17-E7 | 时间作为资源/消耗品机制 | 文档明示"暂不纳入，留作探索" | P3 | ⏸ |
| 17-维护 | 清理 17 号文档内残留的季节机制过时说明 | 季节已被方案 23 移除；已清理 E1/E6/E11 条目并补更新日志 | P2 | ✅ 2026-07-27 |
| 开局-可选 | 改简介/主角后"一键重生成开场"入口 | 文档明示"本次不做，留作未来可选" | P2 | ⬜ |

### 2.6 产品级长期短板（来源：docs/20 对标矩阵，多数未实现；**多模型 ✅、多插入位 Lorebook ✅ 已落地**，详见 docs/20 §5 P0-1/P0-2）

| 事项 | 竞品参照 | 备注 |
|---|---|---|
| 多模型支持 | SillyTavern 20+ | ✅ **已实现**（早于本期工作）：`providers.js` 抽象层（6 预设：DeepSeek/通义/智谱/Ollama/OpenAI兼容/自定义）+ `index.html` #providerSelect 下拉 + `storage.applyProviderPreset` 选方自动填 Base URL/模型并持久化；`llm.js` 的 `callLLM*` 全走 `getProvider().buildBody` 按提供方整形。已补 `test/providers.test.js`（7 项）锁定路由逻辑。Claude(Anthropic) 非 OpenAI 兼容格式，未含，见 ❓3。 |
| 多插入位 Lorebook（system/author note/before user/after user 四槽位） | SillyTavern 5 处 | ✅ **已实现**（早于本期工作）：`insert_at` 四槽位贯穿 rag→utils→prompt→llm→lore-ui，见 docs/20 §5 P0-2（2026-07-15） |
| ANN 索引持久化 | — | ✅ **已实施**（2026-07-28，路径 A 真持久化，见 #14 / docs/39）；并已在设置界面加「清除索引缓存」入口（手动清理孤儿索引） |
| 记忆可视化时间线增强 | AI Dungeon | 纯前端可做（见 doc 20 P1-1 / doc 21 时间系统进阶）；非依赖平台化 |
| 跨会话长期记忆 | 猫箱/筑梦岛 | 已有纯本地记忆包导入导出兜底，云化与零后端定位冲突 |
| 群聊/多角色共演 | SillyTavern/猫箱 | 现仅 NPC 活动（🔶） |
| 主动互动/动态推送 | 筑梦岛 | 事件引擎目前是被动的 |
| 多模态（语音/生图） | 星野/猫箱 | 远期 |
| UGC 社区/角色市场 | 猫箱 | 依赖平台化（Phase 6） |

### 2.7 待人工确认项

| # | 事项 | 说明 |
|---|---|---|
| ✅1 | ~~retrigger_policy 具体落点~~ | 已解决（UI-5 / docs/44 范围A，2026-07-29）：编辑器落 `src/lore-editors.js`（截止事件 retrigger_policy：`once` \| `repeatable`+`max_repeats`+`cooldown_steps`），归一化在 `src/store.js:242`，消费判定在 `src/triggers.js:63`，配 `test/44-time-system-advanced.test.js` 14 项测试 |
| ✅2 | ~~B4 羁绊范围选项 A/B/C~~ | 已决策（2026-07-29）：维持现状「不兼容旧档」——读档仅兜底、不回填老档，老档关系页签只显示文字层、无数值条；用户将重新生成世界，旧档兼容无意义。A 全量功能（编辑器+数值+选项驱动+展示+测试）已落地，448 测试通过 |
| ✅3 | ~~Claude(Anthropic) 是否纳入多模型~~ | 已决策（2026-07-29）：暂不纳入。当前 6 预设（DeepSeek/通义/智谱/本地 Ollama/OpenAI/自定义）均 OpenAI 兼容格式；Claude 需独立 `apiStyle:"anthropic"` 分支与独立流式解析（中工作量/中风险），用户多为非技术，暂不值得。API 配置弹窗已加提示「Claude 暂未接入」 |

---

## 三、代码可优化点（按优先级）

### 3.1 P0 · 结构性债务

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 1 | ~~**4 组循环依赖**：utils↔game、game↔render、llm↔game（llm 导入 processTurn）、rag↔prompt~~ | `src/` | ✅ 2026-07-27 全部断开：① utils↔game、theme↔game、llm↔game（processTurn）、game↔render（render 导入的 chooseOption/confirmRestart 只在 HTML 字符串里出现，按钮由 app.js data-action 事件接线分发）四处均为死导入，直接删除；② rag↔prompt：getWorldLoreKB 是纯状态读取器，移入 store.js，prompt/llm/lore-ui/rag 改从 store 导入；③ render↔save（拆重导出层后显形）：abortCurrentRequest 移入 turn-lifecycle.js（依赖注入风格，调用方传 S），保持该模块零 import | 
| 2 | **超大文件**：~~lore-ui.js 2043 行、`processTurn` 单函数 306 行~~；render.js 1407、llm.js 1341、prompt.js 1316 待定 | 同左 | ✅ 2026-07-27 第一批完成：① lore-ui.js 拆为核心面板 906 行（知识库树/修订确认/时间体系表单）+ lore-graph-ui.js 481 行（图谱视图，经 setOpenNodeInKB 回调注入保持单向依赖）+ lore-editors.js 681 行（规则/人物/变量/物品四编辑器），categoryColor 纯函数移入 kg-graph.js，app.js 导入源同步；② processTurn 拆为编排器 + 6 个阶段函数（buildPendingEntry / handleInjectionBlocked / callTurnLLMWithRetry / applyNormalTurn / renderTurnOutcome / recordTurnErrorLog），8 场景集成测试兜底。render/llm/prompt 是否继续拆需评估收益后再定 |
| 3 | **测试覆盖倒挂**：最大的 lore-ui.js / render.js / save.js / storage.js 几乎无测试，而纯函数模块覆盖很好 | test/ | ✅ 2026-07-27 已补 processTurn 集成测试（test/process-turn.test.js，8 个场景：正常回合/空选项兜底/并发锁/注入拦截/非故事响应/网络错误/坏 JSON 重试/会话失效），并顺带抓出并修复 2 个生产 bug（见更新日志）；lore-ui/render/save/storage 的单测仍待补 |
| 4 | ~~game.js 的历史重导出适配层造成双导入路径~~ | `src/game.js` | ✅ 2026-07-27 已删除：app.js/render.js 改为直接从 save.js/lore-ui.js/critic.js 导入；game.js 只保留自身用到的 5 个跨模块引用 |

### 3.2 P1 · 重复代码消除

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 5 | ~~"JSON.parse → 正则截取 → tryRepairJSON" 三段式宽松解析重复 ≥3 次~~ | `llm.js` | ✅ 2026-07-27 已抽 `parseJsonLoose(text, {expectArray})`（留在 llm.js，唯一消费方），三处改用 |
| 6 | ~~主线程 transformers 回落加载逻辑逐行重复两遍~~ | `rag.js` | ✅ 2026-07-27 已抽 `ensureMainThreadTransformers()` 共用，两处改用 |
| 7 | init 中 4 段相同的 fetch+catch、4 段相同的 idbGet+JSON.parse | `app.js` init | ✅ 2026-07-27 已抽 `fetchDataSafe(url, fallback, asText)` / `idbGetJson(key, fallback)` |
| 8 | 隐藏徽章模式（display=none + innerHTML 清空）在同一函数区重复 5 次 | `lore-ui.js` updateTimeConflictBadge/updateStartDateWarn | ✅ 2026-07-27 已抽 `setBadge(el, html)` 小工具 |

### 3.3 P1 · 状态与错误处理

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 9 | 单例 S 有 40+ 字段，混合运行时状态 / UI 状态 / 缓存 / 调试数据，全模块自由读写，无变更通知 | `store.js` | 短期：按注释分区（runtime / ui / cache / debug）；中期：高频渲染字段加轻量订阅（改字段自动触发对应 render），减少"漏刷"。**✅ 2026-07-28 短期分区已落地**（`store.js` S 对象加 A 运行时/B 缓存/C UI/D 调试/E 临时编辑缓冲 五区注释）；**中期订阅系统未做，保持待评估**（架构级改动，需单独确认） |
| 10 | 错误处理不统一：约 25+ 处 catch→console.warn 降级、多处空 catch、game.js 有裸吞错 | `game.js` L140/L259/L1091、`save.js` L24/28 等 | **✅ 2026-07-28 最小侵入版落地**：`utils.js` 新增 `logError(scope, err)`（默认静默 console.warn + 写入 `S.debugLog.chunkErrors` 诊断缓冲，**绝不弹 toast**）；已替换 `game.js`/`llm.js` 共 6 处"辅助 LLM 调用失败→静默兜底"的 console.warn 为 `logError`（一致性包/角色卡/世界观裁判/知识库修订/Phase3 审稿/游戏内一致性包）。**其余 catch（含有意空 catch、需 throw 或弹 toast 的关键路径如网络重试/存档回滚/导入报错）保留不动**——避免把"有意静默降级"变成烦人弹窗；后续按同模式渐进替换，不一次性改动以防回归 |

### 3.4 P2 · 性能

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 11 | 世界/存档列表 innerHTML 全量重建 | `render.js` renderWorldList L394、renderSaveList L437 | 列表增量更新或至少 keyed diff；数量少时优先级不高 |
| 12 | 存档时对 state/history 全量 JSON 序列化再反序列化（当 deepClone 用） | `save.js` L215-241 | **✅ 2026-07-28 已确认落地**：`utils.js::deepClone` 本就是 `structuredClone` 优先 + `JSON.parse(JSON.stringify())` 兜底（现代浏览器/Node17+ 直接走 structuredClone）；`save.js` 关键路径（L203-209）已复用预序列化串，避免重复 stringify。无需改代码 |
| 13 | ~~styles.css 首行 @import Google Fonts~~ | `styles.css` L2 | ✅ 2026-07-27 已移除 @import，改系统字体栈优雅回退（中文网络字体动辄数十 MB 不宜内置）；CSP 同步移除 fonts.googleapis.com / fonts.gstatic.com |
| 14 | ANN 索引不持久化，每次刷新重建（同 2.6） | `ann-index.js` | **✅ 2026-07-28 已实施（路径 A·真持久化）**：用 hnswlib 自带 IDBFS（IndexedDB 虚拟文件系统）落盘；文件名含内容指纹（`ann_<worldId>_<dim>_<fp>_v1.idx`），刷新 `loadIndex` 读回跳过重建，编辑片段后 fp 变自动失效重建；任一步失败静默降级、由 `rag.js` O(n) 兜底。详见 `docs/39`。改动仅 `src/ann-index.js`；`rag.js`/`lore-ui.js`/`save.js` 失效钩子无需改（fp 自动处理）。新增 `test/41-ann-persist.test.js`（3 项） |

### 3.5 P2 · 其他

| # | 问题 | 位置 | 建议 |
|---|---|---|---|
| 15 | styles.css 2647 行单文件无分层 | `styles.css` | **✅ 2026-07-28 已加分层索引**：顶部 `LAYER MAP` 大注释（基础/组件/屏幕/重设计层/媒体查询 五层 + 行号区间）+ 3 处屏幕层横幅（主界面/列表屏幕/游戏界面）；不物理拆分（无构建步骤） |
| 16 | tools/ 下调试截图与 debug-*.mjs 混在工具脚本里 | `tools/` | **✅ 2026-07-28 已移入 `tools/_archive/`**：`debug-backdrop/card-transparency/mobile-overlap/worldlist` 四个 `.mjs` + `_dbg_detail/_dbg_list` 两张 PNG；已 grep 确认全项目零引用（不影响构建/测试），其余正式工具（verify-modules/syntax_check/browser-smoke/regression_test/recompute-csp-hash/_ui_capture/各 embeddings 脚本）保留原位 |
| 17 | `theme.js::toggleTheme` 彩蛋会向用户输入框预填一段"越狱提示词"文本 | `theme.js` L203-221 | 用户决定保留，不处理（2026-07-30）：该功能为用户有意保留的彩蛋，不视为待修复项 |

---

## 四、建议路线图

1. **快赢批次（1 天内）**：✅ #7/#8 小重复消除、#13 字体本地化、17 号文档过时说明清理（均 2026-07-27 完成）。
2. **稳固批次（先测后改）**：✅ #3 processTurn 集成测试 → ✅ #1 循环依赖全部消除（含 llm↔game、game↔render、rag↔prompt、render↔save）→ ✅ #4 删重导出层 → ✅ #5/#6 抽公共函数 → ✅ #2 超大文件拆分第一批（lore-ui 三分 + processTurn 阶段化）（均 2026-07-27 完成）。稳固批次收官；render/llm/prompt 是否继续拆列入待评估。
3. **功能批次（按价值）**：多模型预设扩展（providers 已就位）→ **Phase 5 工具调用约束 + L2 分层缓存 ✅（2026-07-28，方案 C）** → **A3 完成度清单 ✅（2026-07-28）** → 29#6 硬扫描 → 21 号 UI-1 ✅（2026-07-27）。
4. **探索批次**：时间进阶其余项（多插入位 Lorebook 已于 §2.6 标 ✅，移出探索批次；**ANN 持久化已于 2026-07-28 实施（见 #14），移出探索批次**）。

---

## 五、更新日志

| 日期 | 变更 |
|---|---|
| 2026-07-27 | 初版：汇总 docs/17/19/20/21/28/29/30-33/B6/知识晋升确认开关/开局剧情/架构评估/ui-redesign 的未完成项，附代码优化点排查（循环依赖/超大文件/重复代码/状态与错误处理/性能）。 |
| 2026-07-27 | 快赢批次落地 3 项：① #13 字体本地化——styles.css 移除 Google Fonts @import，--serif 补思源宋体/SimSun 回退、body 字体栈补雅黑、.home-title 改用 var(--serif)，index.html CSP 移除 fonts.googleapis.com/fonts.gstatic.com；② #7 app.js init 抽 fetchDataSafe/idbGetJson 消除 4+4 段重复，行为不变（各文件独立降级）；#8 lore-ui.js 抽 setBadge 消除 6 处显隐重复；③ 17-维护——docs/17 季节过时说明清理（E1/E6/E11 + 顶部说明 + 表格断行修复）。npm run verify 验证通过。 |
| 2026-07-27 | 稳固批次落地 4 项：① #3 新增 test/process-turn.test.js（真实链路集成测试，只桩 DOM 与 fetch，8 场景全绿），**顺带抓出并修复 2 个生产 bug**：a) game.js processTurn 的 liveIndex/pendingEntry 声明在 try 内、catch 块引用越界——任何回合报错都会让 catch 自身抛 ReferenceError 掩盖真实错误，已提升到 try 之前；b) render.js replaceEntryDOM 误用 removeLogEntry，定稿时把刚生成的回合从 conversationHistory 数组里删掉（剧情数据丢失），已拆出只删 DOM 的 removeEntryDOMOnly；② #1 删 utils.js/theme.js 对 game.js 的死导入，utils↔game、theme↔game 两组循环依赖断开；③ #4 删 game.js 历史重导出层，app.js/render.js/test 改直接从 save.js/lore-ui.js/critic.js 导入；④ #5/#6 抽 llm.js parseJsonLoose（三处宽松解析共用）与 rag.js ensureMainThreadTransformers（两处回落共用）。npm run verify 全绿（371 测试 = 363 基线 + 新增 8）。 |
| 2026-07-27 | #1 循环依赖清零：① llm↔game——llm.js 的 processTurn 是死导入（仅注释提及），删除；② game↔render——render.js 导入的 chooseOption/confirmRestart 只在 data-action HTML 字符串里出现（实际由 app.js 事件接线分发），删除；③ render↔save（拆重导出层后显形）——abortCurrentRequest 只操作 S，移入 turn-lifecycle.js 并改为依赖注入风格（调用方传 S，保持该模块零 import），save/game/render 五处调用点同步；④ rag↔prompt——纯状态读取器 getWorldLoreKB 从 rag.js 移入 store.js，prompt/llm/lore-ui/rag 四处消费方改从 store 导入，顺带删 rag.js 对 prompt.js 的死导入 buildTurnUserMessage。src/ 内循环依赖归零，npm run verify 全绿（371 测试 + 烟雾测试）。 |
| 2026-07-27 | #2 超大文件拆分第一批：① lore-ui.js 2039 行按机械搬移（临时脚本 + 行号锚点校验）三分——lore-ui.js 906 行（知识库树/修订确认/时间体系/开局编辑核心面板）、lore-graph-ui.js 481 行（图谱渲染与交互，反向调用 openNodeInKB 改为 setOpenNodeInKB 回调注入，依赖保持单向）、lore-editors.js 681 行（规则 DSL/人物卡/变量/物品四编辑器，完全自包含）；categoryColor + CATEGORY_COLOR_SEED 移入纯函数模块 kg-graph.js；app.js 导入拆两行，外部消费方（game.js、测试）无需改动。② game.js processTurn 306 行拆为编排器 + 6 阶段函数：buildPendingEntry（待生成条目）、handleInjectionBlocked（注入拦截记录）、callTurnLLMWithRetry（含 2 次重试与流式回调）、applyNormalTurn（状态应用/规则 DSL/世界观守卫/入历史）、renderTurnOutcome（定稿渲染，合并原 didStream 重复分支）、recordTurnErrorLog（错误日志）；编排器保留会话失效校验/选项持久化/B5 修订触发等关键注释与 catch 回填逻辑。npm run verify 全绿（371 测试 + 烟雾测试）。 |
| 2026-07-27 | 用户复查批次（做 1-3）：① **#1 多模型支持核实落地**——抽象层/下拉/自动填充早于本期工作已存在（`providers.js` 6 预设 + `index.html` #providerSelect + `storage.applyProviderPreset`），非未实现；新增 `test/providers.test.js`（7 项纯函数回归）锁定按 Base URL 路由提供方逻辑，测试总数 371→**378**。docs/34 的 2.6 多模型行改 ✅，❓3 登记 Claude(Anthropic) 是否纳入（非 OpenAI 兼容，需独立 API 分支）。③ UI-1 自定义历法可视化编辑器：依 docs/21 第 56 行规矩**先出方案文档**（`docs/35_UI-1自定义历法可视化编辑器方案.md`），待用户确认范围后再动代码（引擎 `calendar.js` 的 `custom_calendar.months` 已支持，仅缺编辑器 UI）。 |
| 2026-07-27 | 用户复查批次（做 1-3）收尾——**UI-1 实施完成（范围 C 完整版）**：编辑器集成进 `src/lore-ui.js` 时间体系卡 custom_calendar 分支（`renderCustomCalendarEditorInner` + `refreshCustomCalendarEditor` + `ccAddMonth/ccDelMonth/ccMoveMonth/ccRenMonthName/ccRenMonthDays/ccRenLabel/ccLeapMonth/ccPreset/ccClearMonths`，拖拽排序用 document 级事件委托）；`src/calendar.js` 新增纯函数 `CUSTOM_CALENDAR_PRESETS/clampCustomCalendarMonths/summarizeCustomCalendar/reorderMonths/insertLeapMonth`；`app.js` 接线 9 个 `cc*` ACTIONS 并在 `timeConfigChanged` 触发显隐刷新；`styles.css` 补 `.time-cfg-cc/.cc-*` 样式；新增 `test/35-custom-calendar-editor.test.js`（9 项纯函数回归）。与引擎解耦，仅读写 `schema.time_config.custom_calendar`，不改推进逻辑。测试总数 378→**387**，verify-modules/syntax 全绿。doc/21 UI-1 行 → ✅，doc/35 补完成说明。 |
| 2026-07-28 | **A3 创作完成度清单/进度卡实施完成（范围 A + 概览顶部内嵌）**：先出方案 `docs/36_A3创作完成度清单进度卡方案.md`（按 UI-1 同款「方案→确认→改代码」流程，用户确认范围 A）；`src/utils.js` 新增纯函数 `computeWorldCompletion(world)`（7 维度：标题/世界观/开场/≥1角色/游玩系统=rules/重要事件=lore_kb 带 trigger 片段/主角设定=hero，返回 `{items,done,total,pct,grade}`，零副作用）；`src/render.js::showWorldDetail` 概览 tab 顶部注入 `.completion-card`（进度条+评级+✓/○ 维度+柔和提示），复用详情页、不新增数据模型、不阻塞游玩；`styles.css` 补 `.completion-*` 一套（复用现有变量）；新增 `test/36-world-completion.test.js`（6 项纯函数回归）。全量 `npm test` **393/393** 通过（387→393），verify-modules/syntax 全绿。doc/28 A3 → ✅（§3/§4/§5 三处），doc/34 A3 行 → ✅ 并同步路线图与更新日志。浏览器烟雾（`check:browser`）本沙箱无本地 Edge/Chrome 可执行文件无法跑，需本机重跑。 |
| 2026-07-28 | **文档核对与修订（依据 docs/38）**：doc/28 §4 的 A2/B1-B4 由 ⬜ 改 ✅（已落地，代码实证）；doc/34 §2.6 多插入位 Lorebook 由"差距（仅 1 处）"改 ✅ 已落地、小标题与探索批次（§三.4）同步修正；doc/20 对标矩阵 §3.1（多插入位/多模型）、§3.2（多模型/Lorebook 插入）、§1.3/§1.5 单模型锁定、§4 模型层、§6 结论"单模型" 等同主题过时标注统一更正为已落地；测试总数补记 **409**（Phase 5 工具调用约束 + L2 缓存，+16，test/37）。 |
| 2026-07-28 | **代码债修复（docs/34 §3.3-§3.5；越狱彩蛋 #17 按用户要求除外）**：① #9 单例 S 短期分区注释落地（`store.js` S 对象 A 运行时/B 缓存/C UI/D 调试/E 临时编辑缓冲 五区），中期订阅系统保持待评估；② #12 确认已落地（`utils.js::deepClone` 本就 `structuredClone` 优先 + `save.js` 复用预序列化串），无需改代码；③ #15 `styles.css` 加 `LAYER MAP` 分层索引 + 3 处屏幕层横幅（主界面/列表屏幕/游戏界面）；④ #16 调试草稿（4 个 `debug-*.mjs` + 2 张 `_dbg_*.png`）移入 `tools/_archive/`（grep 全项目零引用）。#10（25+ 处 catch 统一）、#11（列表 keyed diff）有回归风险，保持待确认；#14（ANN 持久化）本回合已实施（见下条）。 |
| 2026-07-28 | **#14 ANN 索引持久化实施完成（路径 A·真持久化）**：先出方案 `docs/39_ANN索引持久化方案.md`（按"方案→确认→改代码"规矩），黎总确认走 A 后实施。改 `src/ann-index.js`：`getLoreAnnIndex` 改用"内容指纹文件名"（`ann_<worldId>_<dim>_<fp>_v1.idx`，`fp=FNV-1a(snippets 的 id+category+title+content+keywords)`）→ 启动 `syncFileSystem("read")` 恢复 IDBFS → `loadIndex` 读回跳过重建；未命中则 `buildLoreIndex` + `writeIndex` + `syncFileSystem("write")` 落 IndexedDB；任一步失败静默降级、`rag.js` O(n) 兜底。探针确认 `loadIndex/writeIndex/saveIndex` 存在于内联 wasm（解码 base64 搜 embind 字符串）。`rag.js`/`lore-ui.js`/`save.js` 失效钩子无需改（fp 自动处理失效）。新增 `test/41-ann-persist.test.js`（3 项：构建写盘/失效读回不重建/编辑改文件名）。全量 `npm test` **412/412** 通过（409→412），verify-modules/syntax 全绿。#14 移出探索批次（§四.4）。 |
| 2026-07-28 | **设置界面「清除索引缓存」入口（#14 配套）**：黎总要求补"清除索引缓存"入口以回收编辑知识库后残留的孤儿 ANN 索引空间。新增 `src/ann-index.js::clearLoreAnnCache()`：利用 hnswlib 模块暴露的 Emscripten `FS`（EXPORTED_RUNTIME_METHODS 含 'FS'）`readdir` 列出 + `unlink` 删除全部 `ann_*.idx`（多挂载点扫描，防 IDBFS 非根挂载），再 `syncFileSystem("write")` 刷盘释放 IndexedDB，并清空内存 `_cache`（下次进世界懒重建）；任何失败静默降级、不影响存档/剧情。`src/app.js` 注册 `clearAnnCache` 处理函数（两次点击确认，禁用原生 confirm）；`index.html` 设置界面「选项」页新增卡片与按钮；`showToast` 反馈删除数量。新增 `test/42-ann-clear-cache.test.js`（2 项：仅删 ann_*.idx/库无 FS 优雅返回 0）。全量 `npm test` **414/414** 通过（412→414），verify-modules/syntax 全绿。 |
| 2026-07-29 | **时间系统进阶 UI-3/4/5 实施完成（范围 A·核心三件套，先出方案 `docs/44` 经黎总确认 A）**：① UI-3 流速比 `sync_rules`——新增引擎纯函数 `calendarDayIndex`/`applySyncRules`/`clampSyncRules`（`src/calendar.js`，`clampTimelineLine` 增 sync_rules 夹紧、`normalizeTimeConfig` 落点），多时间线卡片内可视化增删 {ref, ratio}（`src/lore-ui.js` `renderMultiverseEditorInner` + `mvAddSync/mvDelSync/mvSyncRef/mvSyncRatio`），游戏主循环推进活动线后按 ratio 同步引用线（`src/game.js`）；② UI-4 默认穿越策略——世界级 `default_timetravel_strategy`（keep/reset/branch，缺省 keep）+ 每线 `timetravel_strategy` 覆盖，`resolveTimeTravelStrategy` 三层（指令>线级>世界级>keep 回落）逆跳时套用，`src/triggers.js`+`src/store.js`+`src/game.js::isBackwardJump`（多世界改用活动线真实历法判定逆跳）+`src/lore-ui.js` `defaultStrategyChanged`/`mvLineStrategy`；③ UI-5 截止事件 `retrigger_policy`——新增「世界时限/截止事件」编辑器（`src/lore-editors.js` `openDeadlineEditor`/`saveDeadlineReview` + `index.html` `deadlineEditorModal` + `src/app.js` 动作），复用 `store.js::normalizeRetriggerPolicy`。**顺带修复 `addCalendar`/`addByTable` 跨闰年 2 月差一天 bug**（静态月表长度未随跨年更新，导致大天数增量多走一天——UI-3 sync 传大 ratio 必踩）；改为 `lenFn(year,mIdx)` 逐年取月长。新增 `test/44-time-system-advanced.test.js`（14 项：calendarDayIndex 增量/addCalendar 闰年回归/applySyncRules/clampSyncRules/resolveTimeTravelStrategy/normalizeRetriggerPolicy/normalizeTimeConfig 落点/clampTimelineLine）。全量 `npm test` **448/448** 通过（434→448），verify-modules/syntax 全绿。doc/21 UI-3/4/5 → ✅，doc/20 §11 补 sync_rules/默认策略层，doc/44 实施状态更新。浏览器烟雾（`check:browser`）仍因沙箱无本地 Chrome 无法跑，需本机重跑。 |
| 2026-07-29 | **UIR-6 取消 + T-2 完成**：黎总决定不再维护《UI界面参考手册》（文件已删），移除 doc 34 / 00-文档导航 / 开发状态与审计手册 / 架构演进与决策史手册 中的 UIR-6 与 UI 手册引用；**T-2 时间系统玩法说明书完成**，新建 `docs/时间系统玩法说明书.md`（面向非技术作者，含穿越 / 多世界 / 流速比 / 截止事件写法示例），落点依 docs/44 建议。 |
| 2026-07-29 | **C-1 农历闰月/节气细化完成（范围 A 完整，先出方案 `docs/45_C1农历闰月节气细化方案.md` 经黎总确认 A）**：节气——新增 `src/calendar.js::SOLAR_TERMS`（12 农历月→[节,中气]）+ `formatCalendarDate(lunar, {showSolarTerms})`（默认关，保引擎层向后兼容；显示层 `theme.js` 开启 → 游戏内日期带「（立春～雨水）」）+ `prompt.js` lunar 分支注入月份→节气映射供 AI 借节气写剧情；闰月——新增 `CUSTOM_CALENDAR_PRESETS.lunarLeap`（含闰二月的 13 月表）+ `lore-ui.js` 编辑器「📅 农历(含闰月)」模板按钮与提示，lunar 模式自身不引入闰月（与 12 月进位冲突、且闰月取决于年份）。新增 `test/45-c1-lunar-solar-terms.test.js`（8 项：SOLAR_TERMS 映射/节气开关/越界回绕/gregorian 不受影响/formatDateOnly/formatDeadlineLabel/lunarLeap 预设）。全量 `npm test` **456/456** 通过（448→456），verify-modules/syntax 全绿。doc/45 实施状态更新；doc/20 §11、doc/34、doc/00、开发状态与审计手册 C-1 → ✅；`docs/时间系统玩法说明书.md` §9 标注已做。浏览器烟雾（`check:browser`）仍因沙箱无本地 Chrome 无法跑，需本机重跑。 |
