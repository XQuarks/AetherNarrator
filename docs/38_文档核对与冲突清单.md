# 文档核对与冲突清单（docs/38）

> 生成日期：2026-07-28
> 性质：跨文档 + 代码实测核对报告，用于长期跟踪"哪些需求真没做、哪些文档已过时、哪些需求冲突"。
> 核对范围：docs/34（项目完善清单）、docs/20（对标分析）、docs/21（时间系统进阶）、docs/28（竞品总览）、docs/29（IP一致性）、docs/17（改进清单）、docs/架构评估与方案脑暴，以及 `src/` 代码实测。

---

## 一、一句话总览

文档34 把**绝大多数真没做的**都列对了，但它自己把**两个已经做掉的功能误标成了"差距"**，而且有几份来源文档（尤其 doc 28、doc 20 的对标矩阵）的"待做清单"严重过时，和代码现实打架。**真正的需求冲突不多，但有一个根冲突贯穿全局**：产品定位"纯前端 / 零后端 / 低门槛" vs 对标猫箱 / 星野那套"平台化 / UGC / 云 / 多模态"。

---

## 二、文档34 自身的两处事实错误（已做却写成没做）

| 错在哪 | 文档34 怎么写的 | 代码真相 | 谁对 |
|---|---|---|---|
| §2.6 多插入位 Lorebook | "当前仅 1 处注入位"（当差距项） | 实际已做：`insert_at` 四槽位（system / 作者注 / 用户输入前 / 用户输入后）贯穿 rag→utils→prompt→llm→lore-ui（满屏 `★P0-2` 注释） | **文档34错** |
| 测试总数 | 更新日志停在 393 | 实际 409（Phase 5 又 +16，架构评估文档和代码都认） | 文档34漏记 |

> 顺带：doc 20 的 §3.1 对标矩阵、§1.3、§1.5 也把"多模型""多插入位"标成 ❌ 差距 / 弱项，但它们自己的 §5（P0-1 / P0-2）早就写 ✅ 已完成——**同一份 doc 20 自己前后打架**，矩阵和短板节已过时。

---

## 三、文档之间的状态冲突（共 6 处，附真相）

| # | 冲突点 | A 文档说 | B 文档 / 代码说 | 真相 |
|---|---|---|---|---|
| 1 | 多插入位 Lorebook | doc34 §2.6 = ❌差距；doc20矩阵 = ❌ | doc20 §5 P0-2 = ✅；代码已做 | **已做** |
| 2 | 多模型支持 | doc20矩阵 = ❌；doc20 §1.5 = "单模型锁定" | doc20 §5 P0-1 = ✅；doc34 = ✅；代码 6 预设 | **已做** |
| 3 | A2 IP检测一致性包 | doc28 §4 = ⬜待做 | doc29（#1 / #3 已做）；doc34 §2.3 = ✅；代码 `detectIp` / `generateConsistencyPack` | **已做** |
| 4 | B1人物卡 / B2变量 / B3背包 / B4羁绊 | doc28 §4 = 全 ⬜ | docs 30-33 + 代码 `world.characters` / `gameState.variables` / `inventory` / `relationships` 都在 | **已做** |
| 5 | 季节机制 | doc20 §1.2 仍写"季节" | doc23 已移除；doc17 E11 = ❌已移除；代码 `delete tc.season`；llm.js 提示词 `{season}` 残留已于 2026-07-28 清理 | **已移除**（含文案残留） |
| 6 | 测试数 393 vs 409 | doc34 日志停在 393 | doc34 正文认 Phase5 完成；架构评估 + 代码 = 409 | **409** |

**最该修的是 doc 28 §4**——它把 A2、B1-B4 全列成"待做"，但这些都是本周刚做完的，等于一份严重过时的清单。

### 3.1 冲突修订状态（2026-07-28 已处理）

> 本表暴露的文档过时标注，已于 2026-07-28 以"代码实测"为准回修来源文档，下列冲突已消除：

| # | 冲突点 | 已修订位置 |
|---|---|---|
| 1 | 多插入位 Lorebook | doc34 §2.6（标 ✅ 已落地）、doc20 §3.1/§3.2/§4 |
| 2 | 多模型支持 | doc20 §1.3/§1.5/§3.1/§3.2/§4/§6、doc34 §2.6 小标题 |
| 3 | A2 IP 检测一致性包 | doc28 §4（⬜→✅） |
| 4 | B1-B4 人物卡/变量/背包/羁绊 | doc28 §4（⬜→✅） |
| 5 | 季节机制 | ✅ 已于 2026-07-28 修复：`src/llm.js:1200` 知识库修订提示词引导句里的 `{season}` 占位符已删除，保留真实占位符 `{calendar_date}`/`{era_label}`（季节机制方案 23 移除后引擎不再替换 `{season}`，残留会导致 AI 生成无法展开的占位符） |
| 6 | 测试数 393 vs 409 | doc34 §五 更新日志已补记 409 |

---

## 四、真正没做的事（按价值分组）

### A. 时间系统进阶（doc 21，除了 UI-1 都未动）
- UI-2 多时间线配置器
- UI-3 流速比 sync_rules（**引擎层都没写**）
- UI-4 分支策略
- UI-5 事件重触发
- UI-6 回溯合并面板
- C-1 农历节气细化、C-2 纪元换算、C-3 时区
- E-1 时间线合并、E-2 事件依赖图、E-3 多世界性能
- T-1 端到端测试、T-2 用户向说明书

### B. 竞品借鉴尾部（doc 28）
- C2 能力目录 / 升级体系
- C4 玩家备注 + 分支预测
- D1 文游 / 小说双模式
- B5' 日程 + 章节化回溯
- C1 模块化世界开关（待确认落点）

### C. IP 一致性（doc 29）
- #6 生成后硬扫描（标注"可选"）

### D. 产品级短板（doc 20 §2.6 / §5）——大部分确实没做
- ~~ANN 索引持久化~~ → **✅ 2026-07-28 已实施（路径 A，见 docs/39 / doc34 #14）**
- 记忆可视化时间线
- 跨会话长期记忆
- 群聊 / 多角色
- 主动互动
- 多模态
- UGC 社区
- 移动端 PWA
- 角色卡 v2 互通
- E2E / CI 测试

### E. 代码债（doc 34 §3.3~§3.5）
- #9 单例 S 40+ 字段无订阅 → **✅ 2026-07-28 已分区注释**（中期订阅系统待评估）
- #10 错误处理不统一（25+ 处裸 catch）→ ⏸ 待确认（回归风险，需先定"哪些弹 toast/哪些静默"）
- #11 列表全量重建 → ⏸ 待确认（优先级低、收益小）
- #12 序列化改用 structuredClone → **✅ 2026-07-28 确认早已落地**（`deepClone` 本就 `structuredClone` 优先）
- #14 ANN 持久化 → **✅ 2026-07-28 已实施（路径 A·真持久化，见 docs/39）**，移出代码债（实质为功能非债）
- #15 styles.css 2647 行无分层 → **✅ 2026-07-28 已加分层索引**
- #16 tools/ 调试脚本混放 → **✅ 2026-07-28 已归档 `_archive/`**
- #17 theme.js 越狱彩蛋（**已做，建议撤**）→ ⏸ 用户除外，未动

---

## 五、没做、但**不建议做**（含"做了反而该撤"的）

| 项目 | 为什么不建议 |
|---|---|
| **E7 时间作为资源 / 消耗品** | doc 17 明文"暂不纳入，与现有'AI 显式推进时间'范式冲突重"——已发的是"时间倒流自由"，和"时间稀缺"本质互斥 |
| **Phase 6 平台化 / 后端 / 云同步 / UGC / 跨会话云记忆** | 与"纯前端零后端"定位直接冲突，只能独立立项另起炉灶 |
| **upload_kind 自动判定 / 版权检测** | doc 29 明确"不可行 / 不碰"——本地无版权库、且当"版权警察"有合规风险 |
| **静态 IP 包注册表（手写克苏鲁 / HP 包）** | 已改为"AI 从用户文本动态生成一致性包"，更通用，静态表没必要 |
| **Claude(Anthropic) 纳入多模型** | 非 OpenAI 兼容，要单独写一套 API 分支 + 流式解析，风险中；非技术用户，DeepSeek / OpenAI 系已覆盖主流（doc 34 ❓3） |
| **C2 能力目录 / 升级体系 + D1 双模式** | doc 28 §6 自己写"数值喧宾夺主""升级体系把轻题材绑成数值游戏，谨慎上"——和"低门槛任意 IP"定位冲突 |
| **开局后"一键重生成开场"入口** | doc 34 标"本次不做，留作未来可选" |
| **theme.js 越狱彩蛋（#17）** | **已经做了但建议撤掉**的反例：向输入框预填"越狱提示词"，与内容安全策略相悖，可能触发模型方风控 |

---

## 六、两个值得拍板的"软冲突"（及其已落实处置 · 2026-07-28）

1. **根冲突（战略层）**：doc 20 / §2.6 把"UGC 社区 / 云同步 / 多模态 / 移动端 / 跨会话云记忆"当成"缺失 = 该补"，但这些在**零后端形态下做不了**。
   **已落实处置**：统一降级为"待商业化立项（Phase 6 平台化）"，不再挂近期待做。标注位置：
   - doc 20 §1.5（L70-73 注"依赖 P2/Phase 6"）、§3.1 矩阵注脚、§4 缺失汇总注、§5 P2 明确"看是否商业化"
   - doc 34 §2.6（L100-106 已注"与零后端冲突 / 依赖平台化"）
   - 下列项均属此列：**跨会话长期记忆、群聊/多角色、主动互动、多模态、UGC 社区、移动端 PWA、角色卡 v2 互通、E2E/CI 测试**（依赖平台化）；**ANN 索引持久化（#14）** 实为功能非债，单列。

2. **需求散落（"时间线可视化 / 回溯 / 分支"规划了 4 遍）**
   **已落实处置**：建统一需求族，指明主追踪文档，各来源加交叉引用，避免重复动工：
   - **A. 世界内时间机制（时间作为游戏机制）** → 主追踪 **doc 21**（UI-2 多时间线 / UI-4 分支策略 / UI-6 回溯合并 / E-1 合并 / E-2 事件依赖图）。*未做，待立项。*
   - **B. 叙事容器时间线 / 日程 / 章节化回溯（玩家可见结构）** → 主追踪 **doc 28 B5**（L52/81/116/144），复用 `calendar.js`+`triggers`+`behavior_records`。*未做，待立项。*
   - **C. 记忆 / 会话可视化时间线（回顾）** → 主追踪 **doc 20 P1-1**（L204），复用 `conversationHistory`。*未做，待立项。*
   - 交叉引用已加：doc 20 P1-1、doc 21 UI-6、doc 28 B5、doc 34 §2.2 B5' 均注"见 docs/38 §六"。
   - **后续若启动**：统一在 doc 21 建追踪项，其余文档标"见 doc 21"，不另起规划。

---

## 七、建议下一步行动（按风险排序）

1. **先修文档（零代码、低风险）**
   - 把 doc 28 §4 的 A2 / B1-B4 划掉（已做）
   - 把 doc 34 §2.6 的"多插入位 Lorebook"划掉（已做）
   - 更新 doc 20 对标矩阵 / 短板节的过时行（多模型、多插入位已做）
   - doc 34 更新日志补 409
2. **撤掉越狱彩蛋（doc 34 #17）**——安全相关，建议直接做。
3. **合并"时间线可视化"重复规划** ✅ 已完成（2026-07-28）：建需求族（A 世界内时间机制→doc21 / B 叙事容器时间线→doc28 B5 / C 记忆可视化→doc20 P1-1），各来源加交叉引用，见 docs/38 §六。
4. **产品级短板（D 组）整体降级** ✅ 已完成（2026-07-28）：统一为"待商业化立项（Phase 6 平台化）"，在 doc 20 §1.5/§3.1/§4/§5 与 doc 34 §2.6 标注。

---

## 八、本文档作为跟踪表的用法

- 每张表的状态以"代码实测"为准，文档标注与代码冲突时，**以代码为准**，并回头修文档。
- 新完成一项，先改对应来源文档（doc 21 / 28 / 29 / 34），再在本表"真相"列打 ✅ 并注明完成日期。
- 本表不替代来源文档；它只负责"暴露不一致"，不负责"记录进度细节"。
